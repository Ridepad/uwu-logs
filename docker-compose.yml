name: uwu-logs

x-uwu-common: &uwu-common
  build:
    context: .
    dockerfile: Dockerfile
  working_dir: /app
  restart: unless-stopped
  environment:
    PYTHONUNBUFFERED: "1"
  # Development bind mount: code edits and generated parser data are shared by
  # all four Python services and remain available on the host.
  volumes:
    - .:/app

services:
  main:
    <<: *uwu-common
    command:
      - gunicorn
      - --bind
      - 0.0.0.0:5000
      - --workers
      - ${UWU_MAIN_WORKERS:-1}
      - --threads
      - ${UWU_MAIN_THREADS:-4}
      - --timeout
      - ${UWU_MAIN_TIMEOUT:-600}
      - --access-logfile
      - "-"
      - --error-logfile
      - "-"
      - Z_SERVER:SERVER
    expose:
      - "5000"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5000/', timeout=5).read(1)"
      interval: 10s
      timeout: 6s
      retries: 10
      start_period: 10s

  upload:
    <<: *uwu-common
    # Keep exactly one worker here: upload progress/chunks are stored in
    # process memory (CURRENT_UPLOADS), so multiple workers would split state.
    command:
      - uvicorn
      - server_upload:app
      - --host
      - 0.0.0.0
      - --port
      - "5010"
      - --workers
      - "1"
      - --proxy-headers
      - --forwarded-allow-ips
      - "*"
    expose:
      - "5010"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5010/upload', timeout=5).read(1)"
      interval: 10s
      timeout: 6s
      retries: 10
      start_period: 10s

  top:
    <<: *uwu-common
    command:
      - uvicorn
      - server_top:app
      - --host
      - 0.0.0.0
      - --port
      - "5020"
      - --workers
      - ${UWU_TOP_WORKERS:-1}
      - --proxy-headers
      - --forwarded-allow-ips
      - "*"
    expose:
      - "5020"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5020/top', timeout=5).read(1)"
      interval: 10s
      timeout: 6s
      retries: 10
      start_period: 10s

  gear:
    <<: *uwu-common
    command:
      - uvicorn
      - server_gear:app
      - --host
      - 0.0.0.0
      - --port
      - "5030"
      - --workers
      - "1"
      - --proxy-headers
      - --forwarded-allow-ips
      - "*"
    expose:
      - "5030"
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5030/docs', timeout=5).read(1)"
      interval: 10s
      timeout: 6s
      retries: 10
      start_period: 10s

  nginx:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      main:
        condition: service_healthy
      upload:
        condition: service_healthy
      top:
        condition: service_healthy
      gear:
        condition: service_healthy
    ports:
      - "${UWU_HTTP_PORT:-8080}:80"
    volumes:
      - ./docker/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s
